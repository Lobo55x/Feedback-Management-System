{
  "name": "feedbackly-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.5"
  },
  "scripts": {
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx,.json",
    "format": "prettier --write ."
  }
}